# Napoleon2025 项目 Claude 使用规范

## 项目背景

博士课题：基于强化学习的支气管镜机器人自动导航系统。
核心功能：实时点云感知、多模式操控（TR/LMB/RMB）、BC 行为克隆训练、AI-Auto 自动导航。

## 对话记录规范

**每次对话结束后，将关键信息记录到 `Chat_log/` 目录，按日期新建文件。**

### 文件命名

`Chat_log/YYYY-MM-DD.md`
如当天已有文件，追加到同一文件末尾，用 `---` 分隔不同会话。

### 记录内容

每条记录包含：
- **时间**：对话发生的日期和大致时段
- **任务**：本次对话解决了什么问题
- **关键决策**：做了哪些重要的设计或代码决策，以及原因
- **修改的文件**：列出变更的核心文件和改动摘要
- **遗留问题**：未解决的问题或下次需要跟进的事项

### 记录模板

```markdown
## [时段] 任务标题

**任务**：简述本次对话的目标

**关键决策**：
- 决策1 — 原因
- 决策2 — 原因

**修改文件**：
- `path/to/file.py` — 改动摘要

**遗留问题**：
- 问题描述（如有）
```

## 其他约定

- 代码修改优先保持与现有风格一致
- 涉及电机控制或 ROS 通信的改动，改前说明影响范围
- BC/RL 训练脚本修改后说明对数据格式的兼容性影响
